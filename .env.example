# No secrets are required to run this project.
# The single optional variable below is only used to speed up or authorize
# Hugging Face model downloads (private models or rate-limit relief).
# Leave it blank for public models such as all-MiniLM-L6-v2.
HF_TOKEN=
